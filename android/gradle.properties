# corvid-android build properties (the android/ Gradle build only; the
# root JVM build reads none of these — Gradle does pick gradle.properties
# up from the project dir given via -p).
android.useAndroidX=true
org.gradle.jvmargs=-Xmx2g
